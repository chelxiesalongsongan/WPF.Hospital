<Window x:Class="WPF.Hospital.DeleteDoctor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Delete Medicine"
        Height="180"
        Width="350"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="40"/>
            <RowDefinition Height="50"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Enter Doctor ID to Delete" 
                   FontWeight="Bold" FontSize="14"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" VerticalAlignment="Center">
            <Label Content="Doctor ID:" Width="80" Background="#FFB9B6B6" FontFamily="Adobe Caslon Pro"/>
            <TextBox x:Name="tbDoctorId" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Delete" Width="80" Margin="0,0,10,0" Background="Red" Foreground="White"
                    Click="DeleteDoctor_Click"/>
        </StackPanel>
    </Grid>
</Window>